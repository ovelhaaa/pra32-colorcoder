name: Phase Scaffold Check

on:
  pull_request:
  push:
    branches:
      - main
      - master

jobs:
  validate-phase-scaffold:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Validate phase scaffold files
        run: ./scripts/check_phase_scaffold.sh
